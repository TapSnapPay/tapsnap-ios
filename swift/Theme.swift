import SwiftUI

enum Theme {
    static let brand = Color(red: 0.231, green: 0.514, blue: 0.965) // #3b82f6
    static let brand600 = Color(red: 0.149, green: 0.388, blue: 0.922) // #2563eb
    static let text = Color.black
    static let muted = Color.gray
    static let card = Color(white: 0.94)
}
